/**
 * Reserved space for a future Google AdSense unit. Intentionally empty for
 * v1 — sizing/placement are decided now so ads can slot in later with no
 * layout change.
 */
export function AdSlot() {
  return <div className="mt-8 h-[250px] w-[300px] max-w-full" aria-hidden="true" />;
}
